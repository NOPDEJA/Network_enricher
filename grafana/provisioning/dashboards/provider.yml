# Loads any dashboard JSON dropped into /var/lib/grafana/dashboards on startup.
apiVersion: 1

providers:
  - name: Network Enricher
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
